[project]
name = "model-backend-py"
version = "0.1.0"
description = "Add your description here"
authors = [
    { name = "Daniel Imfeld", email = "daniel@imfeld.dev" }
]
dependencies = [
    "fastapi>=0.103.1",
    "transformers>=4.33.1",
    "python-multipart>=0.0.6",
    "uvicorn>=0.23.2",
    "torch>=2.0.1",
    "numpy>=1.24.4",
]
readme = "README.md"
requires-python = ">= 3.8"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true
dev-dependencies = [
    "black>=23.9.1",
]

[tool.hatch.metadata]
allow-direct-references = true
